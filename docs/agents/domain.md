# 领域文档

本文件说明工程技能在探索代码库时应如何读取本仓库的领域文档。

## 探索前读取

- 根目录 `CONTEXT.md`
- 与当前改动相关的 `docs/adr/` 架构决策记录

如果这些文件不存在，静默继续；不要因为缺失而中断任务，也不要主动要求创建。

## 布局

本仓库使用单上下文布局。

## 使用术语表中的词汇

当输出中需要命名领域概念时，使用 `CONTEXT.md` 中定义的术语。如果概念缺失，可以记录为后续文档补充点。

## 标记 ADR 冲突

如果输出与已有 ADR 冲突，需要明确指出，而不是静默覆盖既有决策。
